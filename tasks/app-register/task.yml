---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: vmartinvega/cf-tools-tim
    tag: "latest"

inputs:
  - name: tools
  - name: repo

outputs:
  - name: out
  - name: keyvalout

run:
  path: tools/tasks/app-register/task.sh
