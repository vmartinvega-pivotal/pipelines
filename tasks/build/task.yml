---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: "3.5.3"

inputs:
  - name: tools
  - name: repo

outputs:
  - name: out
  - name: keyvalout

run:
  path: tools/tasks/build/task.sh
